KoKi_YJ_decisions_categories = {
	test = {
		allowed = {
			always = yes
		}
		visible = {
			is_debug = yes
		}
		available = {
			always = yes
		}

		complete_effect = {
			set_victory_points = {
				province = 11959
				value = 100
			}
		}

	}
	KoKi_YJ_TAG_decisions_0 = {
		activation = {
			OR = {
				original_tag = USA
				original_tag = ENG
				original_tag = FRA
				original_tag = SOV
				original_tag = GER
				original_tag = ITA
				original_tag = BRA
				original_tag = FIN
				original_tag = JAP
				original_tag = PRC
				original_tag = ROM
				original_tag = MEX
				original_tag = HUN
				original_tag = PHI
				original_tag = BEL
				original_tag = SWI
				original_tag = RAJ
				original_tag = CAN
				original_tag = TUR
			}
			date < 1936.1.2
		}
		available = {
			always = no
		}
		visible = {
		}
		is_good = no
		days_mission_timeout = 10
		fire_only_once = yes
		timeout_effect = {
			KoKi_YJ_TAG_decisions_0_scripted_effects = yes
			set_global_flag = KoKi_YJ_TAG_decisions_YJ_1
		}
		priority = 99999
	}
	KoKi_YJ_TAG_decisions_1 = {
		activation = {
			OR = {
				original_tag = USA
				original_tag = ENG
				original_tag = FRA
				original_tag = SOV
				original_tag = GER
				original_tag = ITA
				original_tag = BRA
				original_tag = FIN
				original_tag = JAP
				original_tag = PRC
				original_tag = ROM
				original_tag = MEX
				original_tag = HUN
				original_tag = PHI
				original_tag = BEL
				original_tag = SWI
				original_tag = RAJ
				original_tag = CAN
				original_tag = TUR
			}
			has_global_flag = KoKi_YJ_TAG_decisions_YJ_1
		}
		available = {
			always = no
		}
		visible = {
		}
		is_good = no
		days_mission_timeout = 15
		fire_only_once = yes
		timeout_effect = {
			KoKi_YJ_TAG_decisions_0_scripted_effects = yes
			set_global_flag = KoKi_YJ_TAG_decisions_YJ_2
		}
		priority = 99999
	}

	KoKi_YJ_TAG_decisions_2 = {
		activation = {
			OR = {
				original_tag = USA
				original_tag = ENG
				original_tag = FRA
				original_tag = SOV
				original_tag = GER
				original_tag = ITA
				original_tag = BRA
				original_tag = FIN
				original_tag = JAP
				original_tag = PRC
				original_tag = ROM
				original_tag = MEX
				original_tag = HUN
				original_tag = PHI
				original_tag = BEL
				original_tag = SWI
				original_tag = RAJ
				original_tag = CAN
				original_tag = TUR
			}
			has_global_flag = KoKi_YJ_TAG_decisions_YJ_2
		}
		available = {
			always = no
		}
		visible = {
		}
		is_good = no
		days_mission_timeout = 695
		fire_only_once = yes
		timeout_effect = {
			KoKi_YJ_TAG_decisions_0_scripted_effects = yes
		}
		priority = 99999
	}

	KoKi_YJ_TAG_decisions_4 = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = FIN
		}
		available = {
			FROM = { NOT = { has_idea = YJ_FIN_5_ideas } }
		}
		visible = {
			original_tag = FIN
		}
		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		cost = 50 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
			FROM = { add_ideas = YJ_FIN_5_ideas }
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_ENG_decisions_1 = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = ENG
		}
		available = {
			has_idea = YJ_ENG_5_ideas
			FROM = { NOT = { has_idea = deterrence } }
		}
		visible = {
			original_tag = ENG
		}
		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		cost = 100 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = { 
			FROM = {
				if = {
					limit = { NOT = { has_idea = deterrence } }
					add_timed_idea = {
						idea = deterrence
						days = 360
					}
				}
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_ROM_decisions_03 = { 
	icon = GFX_decision_generic_form_nation
	allowed = {
		original_tag = ROM
	}
	available = {
			has_idea = YJ_ROM_3_ideas
			FROM = { NOT = { has_idea = YJ_ROM_03_ideas } }
	}
	visible = {
		original_tag = ROM
	}
	target_trigger = {
		FROM = {
			is_in_faction_with = ROOT
		}
	}
	cost = 100 #决议需要的花费
	days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
	days_re_enable = 0
	modifier = { #填入修正效果
	}
	complete_effect = { 
		FROM = {
			if = {
				limit = { NOT = { has_idea = YJ_ROM_03_ideas } }
				add_timed_idea = {
					idea = YJ_ROM_03_ideas
					days = 60
				}
			}
		}
	}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_ROM_decisions_04 = { 
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = ROM
		}
		available = {
			has_idea = YJ_ROM_4_ideas
			FROM = { NOT = { has_idea = YJ_ROM_04_ideas } }
		}
		visible = {
			original_tag = ROM
		}
		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		cost = 100 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = { 
			FROM = {
				if = {
					limit = { NOT = { has_idea = YJ_ROM_04_ideas } }
					add_timed_idea = {
						idea = YJ_ROM_04_ideas
						days = 60
					}
				}
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	#通用抽奖

	#第一次抽奖逻辑

	KoKi_YJ_TY_decisions_1 = {
		activation = {
			date < 1936.1.2
		}
		available = {
			always = no
		}
		visible = {
		}
		is_good = no
		days_mission_timeout = 30
		fire_only_once = yes
		timeout_effect = {
			if = {
				limit = { 
					is_ai = no
				}
				hidden_effect = {
					if = {
						limit = {
							has_country_flag = YJ_controller
						}
						every_country = {
							limit = { 
								is_ai = no
							}
							set_country_flag = YJ_get_idea
						}
						while_loop_effect = {
							limit = {
								any_country = {
									has_country_flag = YJ_get_idea
								}
							}

							random_country = {
								limit = {
									has_country_flag = YJ_get_idea
								}
								YJ_TY_scripted_effects = yes
								clr_country_flag = YJ_get_idea
							}
						}
					}
				}
			}
			set_global_flag = KoKi_YJ_decisions_YJ_1
		}
		priority = 99999
	}

	#第二次抽奖逻辑
		KoKi_YJ_TY_decisions_2 = {
		activation = {
			has_global_flag = KoKi_YJ_decisions_YJ_1
		}
		available = {
			always = no
		}
		visible = {
		}
		is_good = no
		days_mission_timeout = 120
		fire_only_once = yes
		timeout_effect = {
			if = {
				limit = { 
					is_ai = no
				}
				hidden_effect = {
					if = {
						limit = {
							has_country_flag = YJ_controller
						}
						every_country = {
							limit = { 
								is_ai = no
							}
							set_country_flag = YJ_get_idea
						}
						while_loop_effect = {
							limit = {
								any_country = {
									has_country_flag = YJ_get_idea
								}
							}

							random_country = {
								limit = {
									has_country_flag = YJ_get_idea
								}
								YJ_TY_scripted_effects = yes
								clr_country_flag = YJ_get_idea
							}
						}
					}
				}
			}
		}
		priority = 99999
	}


	#75pp50次抽奖
		KoKi_YJ_decisions_pp = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = yes
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = KoKi_YJ_decisions_pp_txt
				check_variable = {
					KoKi_YJ_decisions_pp < YJ_country_limit
				}
			}
			date > 1937.1.1
		}
		visible = {
			
		}
		cost = YJ_country_cost #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    hidden_effect = {
			    KoKi_YJ_decisions_pp = yes
				add_to_variable = {
					KoKi_YJ_decisions_pp = 1
				}
				set_variable_to_random = { var = YJ_rng min = 1 max = 21 }
				round_variable = YJ_rng
				log = "value1 : [?YJ_rng]"
		    }
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_decisions_YSC = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = no #暂停使用
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = KoKi_YJ_decisions_pp_txt
			}
			has_equipment = { convoy > 1499 }
		}
		visible = {
			OR = {
				original_tag = PHI
				original_tag = BEL
			}
		}
		cost = 0 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    add_equipment_to_stockpile = { 
				type = convoy_1
				amount = -1500
			}
		    hidden_effect = {
			    KoKi_YJ_decisions_pp = yes
				set_variable_to_random = { var = YJ_rng min = 1 max = 21 }
				round_variable = YJ_rng
				log = "value1 : [?YJ_rng]"
		    }
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_decisions_pp_WX = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = no #暂停使用
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = KoKi_YJ_decisions_pp
			}
			date > 1937.1.1
		}
		visible = {
			OR = {
				original_tag = SWI
				original_tag = AUS
				original_tag = CZE
				AND = {
					original_tag = HUN
					has_idea = YJ_HUN_2_ideas
				}
			}
		}
		cost = 60 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    hidden_effect = {
			    KoKi_YJ_decisions_pp = yes
				add_to_variable = {
					KoKi_YJ_decisions_pp = 1
				}
				set_variable_to_random = { var = YJ_rng min = 1 max = 21 }
				round_variable = YJ_rng
				log = "value1 : [?YJ_rng]"
		    }
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}
	
	KoKi_YJ_decisions_4 = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = yes
		}
		available = {
			is_ai = no
		}
		visible = {
		}
		cost = 0 #决议需要的花费
		days_remove = 30  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
			experience_gain_air = 100
			experience_gain_army = 100
			experience_gain_navy = 100
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    army_experience = 500
			navy_experience = 500
			air_experience = 500
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		    add_mastery_bonus = {
				bonus = 1000
				days = 1460
				name = YJ_mastery_bonus
				folder = land
			}
			add_mastery_bonus = {
				bonus = 1000
				days = 1460
				name = YJ_mastery_bonus
				folder = naval
			}
			add_mastery_bonus = {
				bonus = 1000
				days = 1460
				name = YJ_mastery_bonus
				folder = air
			}
			army_experience = 500
			navy_experience = 500
			air_experience = 500
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}
	KoKi_YJ_decisions_5 = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = yes
		}
		available = {
			is_ai = no
		}
		visible = {
			NOT = {
				has_country_flag = YJ_resource_get_flag
			}
		}
		cost = 75 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
			hidden_effect = {
				YJ_ZY_scripted_effects_1 = yes
				set_country_flag = YJ_resource_get_flag
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}



	#将领抽奖

	KoKi_YJ_decisions_JL = { #
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = yes
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = KoKi_YJ_decisions_JL_txt
				check_variable = {
					KoKi_YJ_decisions_JL < 25
				}
			}
		}
		visible = {
            always = yes
		}
		custom_cost_text = decision_cost_CP_25
		custom_cost_trigger = {
			command_power > 24
		}
		cost = 0 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    add_command_power = -25
			hidden_effect = {
				add_to_variable = {
					KoKi_YJ_decisions_JL = 1
				}
				YJ_JL_LJ = yes
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}

	KoKi_YJ_decisions_7 = {
        icon = infiltrate_state
		priority = 1
        target_array = controlled_states
        state_target = yes 
        on_map_mode = map_only
		cost = 100

		visible = {
			is_ai = no
		}

        target_trigger = {
            FROM = {
                is_fully_controlled_by = ROOT
                OR = {
                    arms_factory > 0
                    industrial_complex > 0
                    dockyard > 0
                }
            }

        }

        visible = {
			FROM = {
                is_fully_controlled_by = ROOT
				is_core_of = ROOT
                OR = {
                    arms_factory > 0
                    industrial_complex > 0
                    dockyard > 0
                }
            }
            is_ai = no
        }

        complete_effect = {
            if = {
                limit = { 
                    from = {
                        arms_factory > 0
                    }
                }
                add_offsite_building = {
                    type = arms_factory
                    level = from.arms_factory_level
                }
                from = {
                    set_building_level = {
                        type = arms_factory
                        level = 0
                    }
                }
            }
            if = {
                limit = { 
                    from = {
                        industrial_complex > 0
                    }
                }
                add_offsite_building = {
                    type = industrial_complex
                    level = from.industrial_complex_level
                }
                from = {
                    set_building_level = {
                        type = industrial_complex
                        level = 0
                    }
                }
            }
            if = {
                limit = { 
                    from = {
                        dockyard > 0
                    }
                }
                add_offsite_building = {
                    type = dockyard
                    level = from.building_level@dockyard
                }
                from = {
                    set_building_level = {
                        type = dockyard
                        level = 0
                    }
                }
            }
        }      
		ai_will_do = {
			base = 10 #ai点选这个决议的意愿
			modifier = {
				original_tag = ENG
				factor = 0
			}
			modifier = {
				original_tag = CAN
				factor = 0
			}
			modifier = {
				original_tag = SAF
				factor = 0
			}
			modifier = {
				original_tag = RAJ
				factor = 0
			}
			modifier = {
				original_tag = AST
				factor = 0
			}
		}
    }   
	speed_button_1 = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = {
		}
		days_re_enable = 30
		
		complete_effect = {
			news_event = {
				id = host.1		# 3 speed
			}
		}
	}

	speed_button_2 = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = {
		}
		days_re_enable = 30
		
		complete_effect = {
			news_event = {
				id = host.2		# 4 speed
			}
		}
	}
	KoKi_YJ_decisions_9 = { 
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
		}
		visible = {
			has_idea = YJ_10001_ideas
		}
		cost = 50    
		days_remove = 0  
		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		complete_effect = { 
			FROM = {
				add_ideas = YJ_10001_ideas
			}
		}
		remove_effect = {  
		}
		
		ai_will_do = {
			factor = 1  
		}
	}
	KoKi_YJ_decisions_10 = { 
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
		}
		visible = {
			has_idea = YJ_HUN_1_ideas
			original_tag = HUN
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		cost = 50    
		days_remove = 0  
		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}
		complete_effect = { 
			FROM = {
				add_ideas = YJ_HUN_1_ideas_FROM
			}
		}
		remove_effect = {  
		}
		
		ai_will_do = {
			factor = 1  
		}
	}

		YJ_60d_resource = {
		activation = {
			has_idea = YJ_11_ideas
		}
		available = {
			always = no
		}
		visible = {
			has_idea = YJ_11_ideas
		}
		is_good = yes
		days_mission_timeout = 60
		fire_only_once = yes

		timeout_effect = {
			hidden_effect = {
				YJ_ZY_scripted_effects_1 = yes
				activate_mission = YJ_60d_resource
			}
			custom_effect_tooltip = YJ_60d_resource_tt
		}
	}

	YJ_bread = {
		activation = {
			has_country_flag = YJ_most_factory_country
			NOT = {
				has_country_flag = YJ_bread_flag
			}
			date > 1938.1.1
		}
		available = {
			num_of_civilian_factories < global.sec_max_civ
			NOT = {
				has_country_flag = YJ_most_factory_country
			}
		}

		is_good = no
		days_mission_timeout = 30

		timeout_effect = {
			add_stability = 0.01
			add_political_power = 1
			custom_effect_tooltip = YJ_bread_tt
			set_country_flag = YJ_bread_flag
			set_cosmetic_tag = YJ_bread_cos
		}
	}
	YJ_no_bread = {
		activation = {
			NOT = {
				has_country_flag = YJ_most_factory_country
			}
			has_country_flag = YJ_bread_flag
		}
		available = {
			num_of_civilian_factories > global.max_civ
		}

		is_good = yes
		days_mission_timeout = 30

		timeout_effect = {
			add_stability = -0.01
			add_political_power = -1
			custom_effect_tooltip = YJ_no_bread_tt
			clr_country_flag = YJ_bread_flag
			drop_cosmetic_tag = yes
		}
	}
}



KoKi_YJ_MEX_decisions_categories = {
	KoKi_YJ_MEX_decisions_categories_0 = { #100pp解锁霸王行动
		icon = GFX_decision_generic_form_nation
		allowed = {
			always = yes
		}
		available = {
		}
		visible = {
			has_war_with = GER
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		cost = 100 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		fire_only_once = yes
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_0_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_2_ideas
				}
			}
		    hidden_effect = {
				MEX_state_YJ_scripted_effects = yes
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}
	KoKi_YJ_MEX_decisions_categories_1 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = GER
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = GER
						}
					}
					AND = {
						is_controlled_by = GER
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = GER
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = GER
						}
					}
					AND = {
						is_controlled_by = GER
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
						days = 90
					}
				}
			}
		}
		
		ai_will_do = {
			factor = 1  
		}
	}
	KoKi_YJ_MEX_decisions_categories_2 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = ITA
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = ITA
						}
					}
					AND = {
						is_controlled_by = ITA
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = ITA
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = ITA
						}
					}
					AND = {
						is_controlled_by = ITA
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
						days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
						days = 90
					}
				}
			}
		}
		
		ai_will_do = {
			factor = 1  
		}
	}
	KoKi_YJ_MEX_decisions_categories_3 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = FRA
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = FRA
						}
					}
					AND = {
						is_controlled_by = FRA
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = FRA
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = FRA
						}
					}
					AND = {
						is_controlled_by = FRA
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
					days = 90
					}
				}
			}
		}
		
		ai_will_do = {
			factor = 0  
		}
	}
	KoKi_YJ_MEX_decisions_categories_4 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = BEL
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = BEL
						}
					}
					AND = {
						is_controlled_by = BEL
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = BEL
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = BEL
						}
					}
					AND = {
						is_controlled_by = BEL
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
					days = 90
					}
				}
			}
		}
		
		ai_will_do = {
			factor = 0  
		}
	}
	KoKi_YJ_MEX_decisions_categories_5 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = JAP
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = JAP
						}
					}
					AND = {
						is_controlled_by = JAP
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = JAP
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = JAP
						}
					}
					AND = {
						is_controlled_by = JAP
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
					days = 90
					}
				}
			}
		}
		
		ai_will_do = {
			factor = 0  
		}
	}

	KoKi_YJ_MEX_decisions_categories_6 = { #计划攻势
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = TUR
			OR = {
				is_in_faction_with = USA
				is_in_faction_with = ENG
				is_in_faction_with = BRA
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_MEX_1_ideas
			original_tag = MEX
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = TUR
						}
					}
					AND = {
						is_controlled_by = TUR
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
			has_war_with = TUR
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = BRA
							is_controlled_by = ENG
							is_controlled_by = MEX
							is_controlled_by = RAJ
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							is_controlled_by = TUR
						}
					}
					AND = {
						is_controlled_by = TUR
						any_neighbor_state = {
							OR = {
								is_controlled_by = USA
								is_controlled_by = BRA
								is_controlled_by = ENG
								is_controlled_by = MEX
								is_controlled_by = RAJ
								is_controlled_by = PHI
								is_controlled_by = CAN
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_MEX_decisions_categories_txt
			effect_tooltip = {
				add_dynamic_modifier = {
					modifier = YJ_MEX_state_1_ideas
					days = 90
				}
			}
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = USA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = ENG
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = BRA
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = MEX
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = RAJ
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = PHI
					days = 90
					}
					add_dynamic_modifier = {
						modifier = YJ_MEX_state_1_ideas
						scope = CAN
					days = 90
					}
				}
			}
		}
		ai_will_do = {
			factor = 0  
		}
	}
}

KoKi_YJ_ROM_decisions_categories = {
	KoKi_YJ_ROM_decisions_categories_1 = { #火
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_war_with = ENG
			has_idea = YJ_ROM_2_ideas
			OR = {
				is_in_faction_with = GER
				is_in_faction_with = ITA
				is_in_faction_with = HUN
                is_in_faction_with = FIN
                is_in_faction_with = JAP
                is_in_faction_with = BEL
                is_in_faction_with = FRA
                is_in_faction_with = SWI
				is_in_faction_with = CZE
				is_in_faction_with = TUR
			}
		}
		custom_cost_text = decision_cost_CP_50
		custom_cost_trigger = {
			command_power > 49
		}
		visible = {
			has_idea = YJ_ROM_2_ideas
			original_tag = ROM
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = GER
							is_controlled_by = ITA
							is_controlled_by = HUN
							is_controlled_by = ROM
							is_controlled_by = FIN
                            is_controlled_by = JAP
                            is_controlled_by = BEL
                            is_controlled_by = FRA
                            is_controlled_by = SWI
							is_controlled_by = CZE
							is_controlled_by = TUR
							
						}
                        OR = {
						any_neighbor_state = {
							is_controlled_by = SOV
						}
                        any_neighbor_state = {
							is_controlled_by = USA
						}
                        any_neighbor_state = {
							is_controlled_by = MEX
						}
                        any_neighbor_state = {
							is_controlled_by = BRA
						}
                        any_neighbor_state = {
							is_controlled_by = RAJ
						}
                        any_neighbor_state = {
							is_controlled_by = ENG
						}
                        any_neighbor_state = {
							is_controlled_by = PHI
						}
                        any_neighbor_state = {
							is_controlled_by = PRC
						}
						any_neighbor_state = {
							is_controlled_by = CAN
						}
                        }
					}
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = ENG
							is_controlled_by = SOV
							is_controlled_by = MEX
							is_controlled_by = BRA
                            is_controlled_by = RAJ
                            is_controlled_by = PRC
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							OR = {
								is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
            OR = {
			has_war_with = ENG
            has_war_with = SOV
            }
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							    is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
						}
						any_neighbor_state = {
                            OR = {
                                is_controlled_by = USA
							    is_controlled_by = ENG
							    is_controlled_by = SOV
							    is_controlled_by = MEX
							    is_controlled_by = BRA
                                is_controlled_by = RAJ
                                is_controlled_by = PRC
								is_controlled_by = PHI
								is_controlled_by = CAN
                            }

						}
					}
					AND = {
                        OR = {
                                is_controlled_by = USA
							    is_controlled_by = ENG
							    is_controlled_by = SOV
							    is_controlled_by = MEX
							    is_controlled_by = BRA
                                is_controlled_by = RAJ
                                is_controlled_by = PRC
								is_controlled_by = PHI
								is_controlled_by = CAN
                        }
						any_neighbor_state = {
							OR = {
								is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -50
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_ROM_decisions_categories_txt
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = GER
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = ITA
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = HUN
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = ROM
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = JAP
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = FRA
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = BEL
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = SWI
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = FIN
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = CZE
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_1_ideas
						scope = TUR
						days = 7
					}
				}
			}
		}
		ai_will_do = {
			factor = 1  
		}
	}
	KoKi_YJ_ROM_decisions_categories_2 = { #冰
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
		}
		available = {
			has_idea = YJ_ROM_2_ideas
			has_war_with = ENG
			OR = {
				is_in_faction_with = GER
				is_in_faction_with = ITA
				is_in_faction_with = HUN
                is_in_faction_with = FIN
                is_in_faction_with = JAP
                is_in_faction_with = BEL
                is_in_faction_with = FRA
                is_in_faction_with = SWI
				is_in_faction_with = CZE
				is_in_faction_with = TUR
			}
		}
		custom_cost_text = decision_cost_CP_100
		custom_cost_trigger = {
			command_power > 99
		}
		visible = {
			has_idea = YJ_ROM_2_ideas
			original_tag = ROM
			FROM = {
				OR = {
					AND = {
						OR = {
							is_controlled_by = GER
							is_controlled_by = ITA
							is_controlled_by = HUN
							is_controlled_by = ROM
							is_controlled_by = FIN
                            is_controlled_by = JAP
                            is_controlled_by = BEL
                            is_controlled_by = FRA
                            is_controlled_by = SWI
							is_controlled_by = CZE
							is_controlled_by = TUR
						}
                        OR = {
						any_neighbor_state = {
							is_controlled_by = SOV
						}
                        any_neighbor_state = {
							is_controlled_by = USA
						}
                        any_neighbor_state = {
							is_controlled_by = MEX
						}
                        any_neighbor_state = {
							is_controlled_by = BRA
						}
                        any_neighbor_state = {
							is_controlled_by = RAJ
						}
                        any_neighbor_state = {
							is_controlled_by = ENG
						}
                        any_neighbor_state = {
							is_controlled_by = PHI
						}
                        any_neighbor_state = {
							is_controlled_by = PRC
						}
						any_neighbor_state = {
							is_controlled_by = CAN
						}
                        }
					}
					AND = {
						OR = {
							is_controlled_by = USA
							is_controlled_by = ENG
							is_controlled_by = SOV
							is_controlled_by = MEX
							is_controlled_by = BRA
                            is_controlled_by = RAJ
                            is_controlled_by = PRC
							is_controlled_by = PHI
							is_controlled_by = CAN
						}
						any_neighbor_state = {
							OR = {
								is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
							}
						}
					}
				}
			}
		}
		state_target = yes
		target_root_trigger = {
            OR = {
			has_war_with = ENG
            has_war_with = SOV
            }
			num_of_controlled_states > 0
		}
		on_map_mode = map_only
		cost = 0    
		days_remove = 7  
		target_trigger = {
			FROM = {
				OR = {
					AND = {
						OR = {
							    is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
						}
						any_neighbor_state = {
                            OR = {
                                is_controlled_by = USA
							    is_controlled_by = ENG
							    is_controlled_by = SOV
							    is_controlled_by = MEX
							    is_controlled_by = BRA
                                is_controlled_by = RAJ
                                is_controlled_by = PRC
								is_controlled_by = PHI
								is_controlled_by = CAN
                            }

						}
					}
					AND = {
                        OR = {
                                is_controlled_by = USA
							    is_controlled_by = ENG
							    is_controlled_by = SOV
							    is_controlled_by = MEX
							    is_controlled_by = BRA
                                is_controlled_by = RAJ
                                is_controlled_by = PRC
								is_controlled_by = PHI
								is_controlled_by = CAN
                        }
						any_neighbor_state = {
							OR = {
								is_controlled_by = GER
							    is_controlled_by = ITA
							    is_controlled_by = HUN
							    is_controlled_by = ROM
							    is_controlled_by = FIN
                                is_controlled_by = JAP
                                is_controlled_by = BEL
                                is_controlled_by = FRA
                                is_controlled_by = SWI
								is_controlled_by = CZE
								is_controlled_by = TUR
							}
						}
					}
				}
			}
		}
		complete_effect = { 
		    add_command_power = -100
		}
		remove_effect = {  
			custom_effect_tooltip = KoKi_YJ_ROM_decisions_categories_1_txt
		    hidden_effect = {
				FROM = {
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = SOV
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = PRC
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = RAJ
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = USA
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = BRA
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = MEX
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = ENG
						days = 7
					}
                    add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = PHI
						days = 7
					}
					add_dynamic_modifier = {
						modifier = YJ_ROM_state_2_ideas
						scope = CAN
						days = 7
					}
				}
			}
		}
		ai_will_do = {
			factor = 1  
		}
	}
}

KoKi_YJ_GER_decisions_categories = {
	KoKi_YJ_GER_decisions_categories_0 = { #
		icon = GFX_decision_generic_consumer_goods
		allowed = {
			always = yes
		}
		available = {
		}
		visible = {
			original_tag = GER
			has_idea = YJ_GER_6_ideas
		}
		target_trigger = {
			FROM = { is_in_faction_with = ROOT }
		}
		cost = 50 #决议需要的花费
		days_remove = 0  #决议点击后直到移除所需的时间（立刻完成的决议不需要写这条）
		days_re_enable = 0
		fire_only_once = yes
		modifier = { #填入修正效果
		}
		complete_effect = {#完成（点选）决议时触发的效果
		    FROM = {
				country_event = KoKi_YJ_GER_event.10001
			}
			if = {
				limit = { NOT = { has_idea = totaler_krieg_economy } }
				add_political_power = -200
				add_ideas = totaler_krieg_economy
			}
		}
		cancel_trigger = { #满足什么条件会立刻取消（中断）此决议，会立刻打断正在读条的决议
		}
		cancel_effect = { #决议被打断时触发的效果（这个效果与其他效果不同，它不会在鼠标悬停于决议上方时显示出来）
		}
		remove_effect = { #决议移除（读条完毕）时触发的效果
		}
		ai_will_do = {
			base = 0 #ai点选这个决议的意愿
		}
	}
}



MEX_integrate_state_decisions_categories = {
	MEX_integrate_state = {
		icon = GFX_decision_generic_operation
		allowed = {
			original_tag = MEX
		}
		available = {
		}
		
		visible = {
			any_owned_state = {
				is_on_continent = south_america
				NOT = {
					is_core_of = ROOT
				}
			}
		}
		state_target = yes
		target_trigger = {
		    FROM = {
		    	is_fully_controlled_by = ROOT
		    	is_on_continent = south_america
		    	NOT = {
		    		is_core_of = ROOT
		    	}
		    }
		}
		on_map_mode = map_and_decisions_view
		cost = 10
		ai_will_do = {
			base = 50
			modifier = {
				factor = 10
				any_owned_state = {
					NOT = {
						is_core_of = ROOT
					}
					is_on_continent = south_america
				}
			}
		}
		fire_only_once = no
		days_remove = 5
		complete_effect = {
			#set_country_flag = BRA_core_decision_active
		}
		remove_effect = {
			#clr_country_flag = BRA_core_decision_active
			FROM = {
				add_core_of = ROOT
			}
		}
	}
}
